version: '3.5'

services:
  udmurtapi:
    build: .
    container_name: udmapi
    # command: py bootstrap.py && uvicorn app.main:app --host 0.0.0.0 --port 2180
    command: uvicorn app.main:app --host 0.0.0.0 --port 2180
    volumes:
      - ./images:/code/images
      - ./data:/code/data
    ports:
      - "2180:2180"